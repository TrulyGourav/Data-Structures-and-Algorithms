## When storing a result, we create a copy instead of saving the original because the original object keeps changing later. Without copying, all stored results would end up reflecting the same final state.

## 